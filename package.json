{
  "name": "registro-choferes",
  "version": "0.1.0",
  "private": true,
  "description": "Registro de Choferes (Offal) — backend Express + frontend React (deploy único).",
  "scripts": {
    "build": "npm --prefix web ci && npm --prefix web run build && npm --prefix server ci --omit=dev",
    "start": "node server/src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
